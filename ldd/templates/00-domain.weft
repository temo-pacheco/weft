@c ======================================================================
@c  00-domain.weft — FRAME. The domain and the problem, in the reader's
@c  language. Pure exposition: NO @o, NO @d. Weaves into the book's front
@c  matter; tangles to nothing. Fill the placeholders and delete these
@c  comments.
@c ======================================================================

\chapter{The domain: <one line naming the world this system lives in>}

<Describe the domain as you would to a smart colleague new to it. What are the
actors, the objects, the events? What law or invariant defines the domain ---
the thing that, if violated, means you no longer have the thing at all?>

\section{The problem}

<State the concrete problem this system solves. What is done today, why it
hurts, and what "solved" looks like. Prefer the user's outcome over the
technical mechanism --- the mechanism belongs in the architecture chapter.>

\section{Vocabulary}

<Define every core term the rest of the book will use. These words become the
glossary entries and the DOMAIN prefixes of your chunk names
(e.g. term ``posting'' → chunks named ``Ledger: post entry''). Every domain
word a chunk name later uses MUST trace back to a definition here.>

\begin{description}
  \item[<term>] <definition>.
  \item[<term>] <definition>.
\end{description}
