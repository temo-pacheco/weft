# LaTeX build intermediates (regenerable from project.weft via weft -w tex)
*.aux
*.log
*.toc
*.out
project.tex
# compiled objects and the demo binary
*.o
ledger
# NOTE: src/ (the tangled C) and project.pdf are kept on purpose — the tangled
# output is what makes `weft --diff` a meaningful check, and the PDF is the book.
# weft weave scratch files
nw??????
texput.log
